/**
 * Seneste propp-lande-z (lokalt, samme som Bobber/line-end).
 * Vandsplas m.m. skal læse herfra — ellers føles kastet altid "fast" ved -2,8
 * selv når proppen lander længere.
 */
export const castTargetLandZRef = { current: -2.8 };
